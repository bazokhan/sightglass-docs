# Plan

## Scope
- Rename the product globally to **Sightglass** while preserving centralized brand configuration.
- Build the technical product home page at `/`.
- Complete the deterministic demo with Queries, Usage, Security, and Health screens.
- Add route-specific metadata and verify navigation, responsive layouts, and copy/search/filter interactions.

## Technical approach
- Reuse the existing semantic design tokens, seeded Acme Cloud data, demo shell, and TanStack routes.
- Keep all data local and deterministic; no backend or authentication.
- Create every linked route before verification and leave generated routing files untouched.
